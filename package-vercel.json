{
  "name": "facebook-webhook-vercel",
  "version": "1.0.0",
  "description": "Facebook webhook for Vercel deployment",
  "main": "api/webhook.js",
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod"
  },
  "dependencies": {},
  "devDependencies": {
    "vercel": "^32.0.0"
  }
}
